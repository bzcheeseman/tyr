git:
  depth: 1
  quiet: true

env:
  - PATH="/usr/local/opt/llvm/bin:${PATH}"

script: mkdir -p build && cd build && cmake .. -DCMAKE_INSTALL_PREFIX=../test && make check install

jobs:
  include:
    - stage: test
      script: cd build && make c_test && make python_test && make x_lang_test

